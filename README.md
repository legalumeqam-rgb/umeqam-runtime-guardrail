### Быстрый запуск

```python
from umeqam_guardrail import UMEQAMGuardrail

guard = UMEQAMGuardrail(threshold=0.36)
profile = guard.profile(
    response="Это абсолютно точно верно и без сомнений.",
    signals={
        "s1": 0.45,
        "s2": 0.28,
        "s3": 0.85,
        "s4": 0.22,
        "s5": 0.35,
        "s6": 0.78,
        "s7": 0.92
    }
)
print(profile)

# Пример вывода:
# {
#     "risk_score": 0.72,
#     "risk_zone": "C",
#     "blocked": true
# }
Если риск в зоне "C" или "D" — можно блокировать ответ или отправить на эскалацию.
text4. **Проверь в окне Preview** (кнопка "Preview" справа от "Edit")
   - Переключись на вкладку **Preview** — код должен выглядеть ровно, с цветной подсветкой (синий/зелёный/серый).
   - Если выглядит криво — удали и вставь заново.

5. **Сохрани**:
   - В поле **Commit message** (внизу окна) напиши:
Fix quick start code example in README (correct formatting and indentation)
text- Нажми зелёную кнопку **Commit changes** (внизу окна).

6. После сохранения обнови страницу (F5) и проверь, что код в README выглядит **красиво** (ровные отступы, подсветка).

Пришли мне скрин главной страницы репо (где виден весь README полностью) или просто напиши «готово, код выглядит нормально».

После этого идём к следующему — созданию демо на Hugging Face (это самое важное для продажи).

Жду скрин или «готово».
